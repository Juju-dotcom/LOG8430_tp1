plugins {
  id "org.sonarqube" version "4.4.1.3373" // INVALID - change to:
  id "org.sonarqube" version "4.4.1.3371" // or another valid version
}

sonar {
  properties {
    property "sonar.projectKey", "Juju-dotcom_LOG8430_tp1"
    property "sonar.organization", "juju-dotcom"
  }
}